# Day 7

## Axis

My understanding:
axis 0 = col
axis 1 = row

## Boolean masking

My understanding:

in NumPy, standard Python logical operators like and, or, and not do not work on arrays. They will raise a ambigious error
To combine multiple conditions in NumPy, you must use bitwise logical operators:

& for AND

| for OR

~ for NOT

: - extracts all the records

p.where() -Gives Row Indices,
returns a tuple containing an array of indices, so [0] - gives the clean list

## argmax

My understanding:
np.argmax() returns the index/position of the maximum value.

## What confused me

extracting based on the condition like above or lower etc
when to use where
syntax
i was able to guess which method to apply but faced the syntax issue

struggled with tasl 7. i was able to guess the approach
i got the max value of performance but took some time to get the row. syntax issue

## What became easier

axis
methods like mean, max and axis guess etc

## AI/ML connection

extracting the row based on the condition

## One thing I can now do without help

few ops like max, argmax, guessing what to apply and get the row with small syntax issue including the boolean masking
